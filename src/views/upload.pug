extends base 


block content 
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(name="title",type="text", placeholder="titName",required,minlength=2)
        input(name="description",type="text", placeholder="description",required,minlength=2)
        input(name="hashtags",type="text", placeholder="hashtags",required)
        input(type="submit",value="Upload")