extends base 
include mixins/video

block content
    form(method="GET")
        input(type="text" name="keyword")
        input(type="submit",value="search")
    each video in videos 
        +video(video)
    